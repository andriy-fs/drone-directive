dist
node_modules
package-lock.json
.vscode

# Emitted by `npm run codegen -w protocol` from schema/messages.bare — reformatting
# it would put every regeneration in conflict with the repo style.
protocol/src/generated
