# @drone-directive/protocol

Shared, dependency-free wire-protocol types for online multiplayer, imported by
both `@drone-directive/client` (the game) and `@drone-directive/server` (the
relay Worker). Types-only plus a couple of constants — no game logic, no runtime
dependencies. See [../.docs/multiplayer.md](../.docs/multiplayer.md) for the design.
